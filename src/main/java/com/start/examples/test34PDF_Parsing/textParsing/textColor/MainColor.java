package com.start.examples.test34PDF_Parsing.textParsing.textColor;

import com.start.examples.test34PDF_Parsing.textParsing.textExtractor.MyPDFText2HTML;
import com.sun.xml.internal.ws.api.ResourceLoader;
import org.apache.pdfbox.contentstream.PDFStreamEngine;
import org.apache.pdfbox.pdmodel.PDDocument;
import org.apache.pdfbox.pdmodel.PDPage;
import org.apache.pdfbox.pdmodel.graphics.state.PDGraphicsState;

import java.io.File;
import java.io.IOException;

public class MainColor {
    public static void main(String[] args) throws IOException {
    /*    PDDocument doc = null;
        try {
            doc = PDDocument.load(new File("src/main/java/com/start/examples/test34PDF_Parsing/datango 3.2.2 System Requirements.pdf"));
            PDPage page = (PDPage) doc.getDocumentCatalog().getPages().get(0);
            MyPDFText2HTML myPDFText2HTML = new MyPDFText2HTML();


            System.out.println(graphicState.getStrokingColor().getColorSpace().getName());
            float colorSpaceValues[] = graphicState.getStrokingColor().getColorSpaceValue();
            for (float c : colorSpaceValues) {
                System.out.println(c * 255);
            }
        } finally {
            if (doc != null) {
                doc.close();
            }
        }*/
    }
}
